# Intentiq — NLU Backend Dependencies
# Install: pip install -r requirements.txt

# ML / NLP
scikit-learn>=1.4.0
numpy>=1.26.0
nltk>=3.8.0          # PorterStemmer (no corpus download needed)

# API
flask>=3.0.0
flask-cors>=4.0.0    # allows React (localhost:5173) to call Flask